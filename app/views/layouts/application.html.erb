<!DOCTYPE html>
<html>
  <head>
    <title>AuthRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <ul>
      <li><%=link_to "Pagina home" %></li>
      <li>Pagina admin</li>
      <li>Pagina supervisor</li>
      <li>Pagina colaborador</li>
      <li>Bem vindo, <%= current_user.email %> (<%= current_user.admin? ? 'Admin' : 'Não admin' %>)</li>
      <li><%=link_to "Sair", destroy_user_session_path, method: :delete %></li>
    </ul>


    <%= yield %>
  </body>
</html>
